spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Prueba_P1
    username: postgres
    password: COMAND
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
        use_sql_comments: true

server:
  port: 8000
